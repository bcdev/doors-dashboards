name: doors
channels:
  - conda-forge
  - defaults
dependencies:
  # Python
  - python >=3.8
  #Required
  - dash
  - dash-bootstrap-components
  - pip
  - plotly
  - waitress
  - xcube >=1.3.0
  - xcube_geodb
  - pip:
    - dash-material-ui
